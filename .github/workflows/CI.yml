name: CI

on:
  pull_request:
    branches:
      - "main"
      - "develop"
  workflow_dispatch:

jobs:
  ruff_check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
